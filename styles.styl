* {
  box-sizing: border-box;
}

body {
  font-family: sans-serif;
  font-size: 16px;
}

ul {
  padding-left: 0;
  list-style-type: none;

  ul {
    padding-left: 1.5em;
    list-style-type: disc;
    font-size: 0.95em;
  }
}
